@{
  Layout = "_Layout";
}
@using DrSillyStringzFactory.Models;

<div class="container">
  <div class="jumbotron">
    <h1>@Model.Name Page</h1>
  </div>
  @if(Model.EngineerMachine.Count == 0)
  {
    <p>@Model.Name has no doctors.</p>
  }
  <ul>
    @foreach (EngineerMachine engineerMachine in Model.EngineerMachine)
    {
        <li>@engineerMachine.Engineer.Name </li>
    }
  </ul>
  <button class="btn btn-outline-dark" onclick="window.location.href='/engineers/addMachine/@Model.EngineerId'">Add a Machine</button>
  <button class="btn btn-outline-dark" onclick="window.location.href='/engineers'">Engineers</button>
  @Html.ActionLink("Delete","Delete", new {id = Model.EngineerId}, new{@class="btn btn-outline-dark"})


</div>